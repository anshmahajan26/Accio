


function one(){
    function greet(name){// Parameters are variables written in the function definition.
        console.log(`hello ${name}`)
    }
    greet("om")  //Arguments are actual values passed when calling the function.
}